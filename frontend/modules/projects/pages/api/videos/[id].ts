import { IncomingMessage, ServerResponse } from 'http';

const videoHandler = (req: IncomingMessage, res: ServerResponse) => {
  // res.status(200).json({ name: 'John Doe' });
};

export default videoHandler;
